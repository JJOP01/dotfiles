(setq custom-file "~/.emacs.custom.el")
(load custom-file)

(menu-bar-mode 0)
(tool-bar-mode 0)
(scroll-bar-mode 0)
(global-display-line-numbers-mode)

;; behavioural keybinds
(setq split-height-threshold 0)
(setq split-width-threshold nil)
